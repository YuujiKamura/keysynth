//! `bench` stub - placeholder while the SoundFont reference comparison
//! harness is built out. Real implementation will reuse `keysynth::synth`
//! (rustysynth + hound are already declared in Cargo.toml for it).

fn main() {
    println!("bench stub");
}
